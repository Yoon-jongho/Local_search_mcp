# ===========================================
# Local Search MCP Server 환경설정
# ===========================================

# 허용된 경로들 (콤마로 구분)
# 절대 경로 또는 홈 디렉토리 기준 상대 경로 사용 가능
# 예시: 
#   절대 경로: /Users/username/Documents,/Users/username/Desktop
#   상대 경로: Documents,Desktop,Projects
ALLOWED_PATHS=Desktop,Documents,Downloads

# 병합 파일이나 임시 파일 저장 경로 (선택사항)
# 설정하지 않으면 ALLOWED_PATHS의 첫 번째 경로를 사용
# OUTPUT_PATH=/Users/username/Desktop/output

# 기본 베이스 경로 (ALLOWED_PATHS가 없을 때 사용)
# DEFAULT_BASE_PATH=Desktop

# 최대 파일 크기 (바이트 단위, 기본: 10MB)
MAX_FILE_SIZE=10485760

# 허용된 파일 확장자 (콤마로 구분, 'all'로 설정하면 모든 확장자 허용)
# 예시: .txt,.md,.js,.json,.py,.html,.css
ALLOWED_EXTENSIONS=.txt,.md,.js,.json,.html,.css,.py,.java,.cpp,.c,.h,.xml,.yaml,.yml,.cs,.vue,.ts,.tsx,.go,.rs,.swift,.kt,.php,.rb

# 서버 정보 (선택사항)
SERVER_NAME=local-search-mcp-server
SERVER_VERSION=1.0.0
SERVER_DESCRIPTION=로컬 파일시스템 검색 및 접근을 위한 MCP 서버

# ===========================================
# 설정 예시들
# ===========================================

# 개발자 설정 예시
# ALLOWED_PATHS=Desktop/Projects,Documents/Code,Downloads
# OUTPUT_PATH=Desktop/merged-files

# 일반 사용자 설정 예시  
# ALLOWED_PATHS=Documents,Desktop,Downloads

# 특정 프로젝트만 허용
# ALLOWED_PATHS=/Users/username/workspace,/Users/username/personal-projects
# OUTPUT_PATH=/Users/username/workspace/output

# 모든 파일 확장자 허용
# ALLOWED_EXTENSIONS=all
